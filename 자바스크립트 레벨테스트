// 동기와 비동기 처리에 대하여 설명하라.
    // 동기 = 요청과 결과가 한자리에서 동시에 처리됨
    // 비동기 = 요청한 자리에서 결과가 주어지지 않음

// 싱글 스레드와 멀티 스레드에 대하여 설명하라
    // 싱글스레드 = 말그대로 "싱글" 하나의 프로세스에서 하나의 스레도로만 실행함
    // 멀티스레드 = 말그대로 "멀티" 하나의 프로세승에서 둘 이상을 실행함 (단, 병렬적으로 작업을 수행하지만 동시에 수행하는것은 아님)

// 쿠키과 세션, 로컬저장소의 차이점에 대하여 설명하라.
    // 쿠키 = 웹사이트에 접속할때 생성되는 정보를 담은 임시파일
    // 세션 = 쿠키와 같이 정보를 담은 임시파일이지만 브라우저 종료시 소멸됨
    // 로컬저장소 = 현재 내가 사용하고 있는 PC 에 저장되는 저장소

// 콜백지옥이 무엇이고 이것을 해결하기 위한 방안은 무엇인지 설명하라.
    // jS 에서 비동기 프로그래밍시 함수의 매개 변수로 넘겨지는 
    // 콜백 함수가 반복되어 코드의 들여쓰기 수준이 감당하기 힘들 정도로 깊어지는 현상
    // Promise, await 를 사용하여 비동기 메서드를 동기메서드 처럼 값을 반환 받는다.

// node.js 이벤트 큐 동작원리에 대하여 설명하라.
    // X

// GET, POST 방식에 대하여 설명하라.
    // get방식 = url 에 파라미터를 붙여서 전송
    // post방식 = body 안에 데이터를 붙여서 전송
    // 차이점 = get은 중요정보를 다루면 안되고 post는 가능하다.

// 자바스크립트의 배열이 실제 자료구조 배열이 아닌데 그 이유는 무엇인가?
    // 무슨말인지 모르겠습니다.

// http 와 https 의 차이점은 무엇인가?
    // http, https 모두 줄임말인데 뒤에 붙은 s 는 Secure 임으로 http는 브라우저로 전송되는 정보가 암호화 되지 않고
    // https 는 암호화 된다.

// 브라우저 렌더링 과정에 대하여 설명하라.
    // 브라우저 접속 -> 서버에서 html,css 등등 리소스를 다운 -> 트리(?) 생성

// 호이스팅이 무엇인지 서술하라.
    // 변수나 함수를 최상단에 위치하게 하여 동일 스코프에서 어디서든 참조할 수 있게 하는것


